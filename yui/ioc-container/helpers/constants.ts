export enum VoiceStateKey {
  OldState = 'OLD_STATE',
  OldStateChannel = 'OLD_STATE_CHANNEL',
  NewState = 'NEW_STATE',
  NewStateChannel = 'NEW_STATE_CHANNEL'
}

export const paramKeyPrefix = '_param'
